---
name: example_two_camera_math
description: "Example using the geecs interface with the base geecs environment and an arbitrary optimization function"
version: "0.1"
dependencies:
  - badger-opt
  - geecs_python_api
interface:
  - geecs