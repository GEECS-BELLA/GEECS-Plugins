function logOut = fLogFilterV01(logDat,scanN,fltPrm,scanP)%% filter out logfile%% logOut = fLogFilterV01(logDat,scanN,fltPrm,scanP)%% logDat: log data% scanN: scan number to be taken, can be like [1:4,8]% fltPrm: N x 3 array, [filter parameter, min, max] for each filter% scanP: % Function Log Reader, ver.1% Kei Nakamura% Created on Wednesday, March 22th 2006.% Modified on %  %function headerfid = fopen(fname);     % file open('name','permission')   lin1 = fgetl(fid);      % read first line (title)numlin1 = double(lin1); % make char to number (tub = 9)tab = find(numlin1==9); % find subscripts = tubtabsiz = max(size(tab));% size gives you a dimension, take bigger onettl = cell(tabsiz+1,2); % define a cell with tabsize+1 * 1 ttl{1,1} = 1;ttl{1,2} = lin1(1:tab(1)-1);for i=1:tabsiz-1    ttl{i+1,1} = i+1;    ttl{i+1,2}=lin1(tab(i)+1:tab(i+1)-1);endttl{tabsiz+1,1} = tabsiz+1;ttl{tabsiz+1,2} = lin1(tab(tabsiz)+1:end);num = fscanf(fid,'%e',[tabsiz+1,inf]);    % read formatted data file,                                        % inf: till the enddat = num';fclose(fid);
