# v1: HASO — the whole configuration lived in constructor kwargs, no image
# section. (Path is a placeholder; the real one names a lab share.)
name: U_HasoLift
image_analyzer:
  class_path: image_analysis.analyzers.HASO_himg_has_processor.HASOHimgHasProcessor
  kwargs:
    wavekit_config_file_path: /configs/wavekit/WFS_HASO4_LIFT.dat
    mask_top: 125
    mask_bottom: 300
    mask_left: 10
    mask_right: 670
scan: {priority: 10, mode: per_shot, save: true, file_tail: .himg}
