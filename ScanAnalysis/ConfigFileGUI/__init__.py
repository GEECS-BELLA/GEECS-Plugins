"""ConfigFileGUI — PyQt5 GUI for viewing, modifying, and creating config YAML files.

Modules
-------
analysis_preview
    Analysis preview dialog for running and visualising image analysis.
config_io
    Device-config I/O layer (load, save, validate, list).
config_editor_panel
    Form-based editor for device configuration files.
config_editor_window
    Main ``QMainWindow`` with tabbed interface.
experiment_editor
    Experiment config editor panel for scan configurations.
field_widgets
    Reusable field widgets for config editing forms.
file_list_panel
    File-list sidebar for device configuration files.
groups_editor
    Groups editor panel for ``groups.yaml``.
main
    CLI entry point.
pipeline_widget
    Pipeline step editor widget.
scan_analyzer_editor
    Library analyzer editor panel for scan configurations.
scan_config_io
    Scan-config I/O layer (load, save, list, detect).
scan_tree_panel
    Tree-view navigation panel for scan configurations.
section_widget
    Collapsible section widget.
yaml_preview
    Read-only YAML preview panel.
"""
